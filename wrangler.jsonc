{
  // 🌴 CharlestonHacks Events Proxy Worker
  // --------------------------------------
  // Fetches and caches Charleston tech/entrepreneurial events every 3 hours.

  "name": "charlestonhacks-events-proxy",
  "main": "src/index.js",
  "compatibility_date": "2024-11-13",

  // ✅ KV storage for caching responses
  "kv_namespaces": [
    {
      "binding": "CACHE",
      "id": "charlestonhacks_events_cache"
    }
  ],

  // 🕒 Auto-refresh every 3 hours via cron trigger
  "triggers": {
    "crons": ["0 */3 * * *"]
  },

  // ⚙️ Environment configuration
  "vars": {
    "TZ": "America/New_York"
  },

  // 🚀 Deployment configuration
  "usage_model": "bundled",
  "compatibility_flags": ["nodejs_compat"]
}
